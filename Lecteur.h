#ifndef DEF_L
#define DEF_L

#include <fstream>
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <vector>

using namespace std;

/**
 * \brief Lit le fichier pour construire la map
 * \param s le contenu du fichier
 * \return le tileset
 */
vector<int> lecteur (string s);

#endif
